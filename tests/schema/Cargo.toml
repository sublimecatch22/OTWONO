[package]
name = "otwono-schema-tests"
description = "Contract tests: every document OTWONO emits must validate against its published JSON Schema."
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]

[dev-dependencies]
otwono-hal = { path = "../../crates/otwono-hal" }
otwono-capability = { path = "../../crates/otwono-capability" }
otwono-llama = { path = "../../crates/otwono-llama" }
otwono-ai = { path = "../../crates/otwono-ai" }
otwono-fetch = { path = "../../crates/otwono-fetch" }
serde_json = "1"
toml = "0.8"
jsonschema = { version = "0.26", default-features = false }
