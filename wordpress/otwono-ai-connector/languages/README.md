# Translations

Every user-visible string in the plugin is wrapped in a translation function
with the `otwono-ai-connector` text domain. Generate a template with WP-CLI:

```
wp i18n make-pot . languages/otwono-ai-connector.pot
```
